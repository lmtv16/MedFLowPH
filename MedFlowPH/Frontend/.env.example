# Optional: absolute API origin for split deploy (empty = same-origin /api)
# VITE_API_BASE_URL=https://api.example.com

# External interactive app URL (Leave empty/commented out to fallback to local /eda route)
# VITE_INTERACTIVE_URL=http://localhost:3000
